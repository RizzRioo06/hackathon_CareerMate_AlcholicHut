############################################################
# Environment Example - CareerMate Backend
# Choose ONE provider: Standard OpenAI OR Azure OpenAI
############################################################

# --- Standard OpenAI / OpenAI-compatible (use these if not using Azure) ---
OPENAI_API_KEY=your_openai_or_provider_api_key_here
# GPT-5 Models Available:
# - gpt-5o (latest and most capable)
# - gpt-5o-mini (faster, more cost-effective)
# - gpt-4o (previous generation, still excellent)
# - gpt-4o-mini (fastest, most affordable)
OPENAI_MODEL=gpt-5o
# If using an OpenAI-compatible provider (e.g., AIMLAPI), set base URL below
# Example: https://api.aimlapi.com/v1
OPENAI_BASE_URL=
# Set to 'true' to force HTTP client instead of OpenAI SDK
# USE_OPENAI_HTTP=false

# --- Azure OpenAI (preferred if using Azure) ---
# Endpoint: https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
# Deployment name you created in Azure (e.g., gpt-5o-deploy)
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2024-06-01

# --- Google Gemini (alternative provider) ---
# GOOGLE_API_KEY=your_google_api_key_here
# GEMINI_MODEL=gemini-1.5-flash
# PROVIDER=gemini # Uncomment to explicitly use Gemini

# --- AI Provider Configuration ---
# Choose ONE provider from the options below

# --- Server ---
PORT=5000
NODE_ENV=development

# --- Database Configuration ---
# MongoDB Connection String (choose one option below)
# Option 1: MongoDB Atlas (Cloud - Recommended)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/careermate
# Option 2: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/careermate

# Optional: Database Configuration (for future use)
# FIREBASE_API_KEY=your_firebase_api_key
# FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_STORAGE_BUCKET=your_project.appspot.com
# FIREBASE_MESSAGING_SENDER_ID=your_sender_id
# FIREBASE_APP_ID=your_app_id

# Optional: Supabase Configuration
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key
